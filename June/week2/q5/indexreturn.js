//Q5. What will be returned if you look for the index of something that does not exist?
var arr = ['a','b','c','d'];

const index = arr.indexOf('e');
console.log(index);
//  If the element does not exist then, returned -1.